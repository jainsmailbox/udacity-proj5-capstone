# Create Load balancer
apiVersion: v1
kind: Service
metadata:
  name: udacity-proj5-capstone-aj
spec:
  selector:
    app: udacity-proj5-capstone-aj
  ports:
    - port: 80
      targetPort: 80
  type: LoadBalancer
